package com.renegatemaster.recipecomposeapp.ui.theme

import androidx.compose.ui.graphics.Color

// Light Theme

// Main
val PrimaryColor = Color(0xFF5F3678) // Categories
val AccentColor = Color(0xFFE13E3E) // Favorites
val AccentBlue = Color(0xFF525DC0) // Portion progress bar

// Slider
val SliderTrackColor = Color(0xFFA1A9F9)

// Background
val BackgroundColor = Color(0xFFF4FAFF) // Main
val SurfaceColor = Color(0xFFFFFFFF) // Cards and dividers
val SurfaceVariantColor = Color(0xFF303030) // Dividers and inactive elements
val DividerColor = Color(0xFFF5F5F5) // Dividers

// Text
val TextPrimaryColor = Color(0xFF000000) // Primary
val TextSecondaryColor = Color(0xFF666666) // Secondary

// Dark Theme

// Main
val PrimaryColorDark = Color(0xFF9A9EFF) // Categories
val AccentColorDark = Color(0xFFE57373) // Favorites
val AccentBlueDark = Color(0xFF64B5F6) // Portion progress bar

// Slider
val SliderTrackColorDark = Color(0xFF7986CB)

// Background
val BackgroundColorDark = Color(0xFF121212) // Main
val SurfaceColorDark = Color(0xFF1E1E1E) // Cards

// Text
val TextPrimaryColorDark = Color(0xFFE6E6E6) // Primary
val TextSecondaryColorDark = Color(0xFFB0B0B0) // Secondary